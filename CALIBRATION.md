# PICO 真机校准流程

本工程已经内置头显内校准面板，但 2026-08-05 验收时没有连接 PICO 真机，因此仓库里的默认值仍是安全起始值，不是真机结论。

## 1. 安装与冷启动

```bash
pico-cli device list --format json
./gradlew installDebug
adb shell am start -n com.example.spatialdemo/.platform.LaunchActivity
```

确认状态栏显示：

- `Tracking: SUCCESS · SUPPORTED`
- `手势敲鼓已开启`，手进入视野后显示“左手已识别”、“右手已识别”或“左右手已识别”
- `授权采样 8/8`
- 没有 `FATAL EXCEPTION`

演奏时不需要手柄。捻合拇指与食指，或自然握拳，再将手向下挥过鼓面。需要防止误触时，可在紧凑 HUD 或“手势与声音”页暂停手势敲鼓。

## 模拟器试听（不替代真机校准）

1. 在 PICO Emulator 窗口按住鼠标右键拖动视角；鼓组按坐姿高度放置，默认水平视角下可能位于画面下方。
2. 点击紧凑 HUD 的“试听军鼓”，无需手部追踪即可验证系统确实有声音。
3. 打开“校准与设置”，可分别试听 Snare、Kick，或按顺序试听整套 8 件鼓。
4. HUD 显示“授权 8/8 · 音频就绪”后再试听；若宿主 Mac 没声音，先检查 PICO Emulator 和 macOS 的输出设备/音量。
5. 模拟器只能验证样本加载、模式开关、按钮调用和左右声像；它不会生成真实手部关节帧，不能用于手势敲击、手势速度、握槌阈值、真实鼓面位置或端到端演奏延迟结论。

## 2. 鼓组整体位置

1. 在紧凑 HUD 点击“调整鼓组位置”。
2. 默认位置会把真实的黑色三脚鼓凳放在用户脚下原点，两个吊嗵鼓位于正前方。使用 X（左右）、Y（上下）和 Z（远近）进行个人微调；`+Z` 表示靠近演奏者。
3. 此调整会同时移动正式模型与全部命中区域，声音不会留在原位。
4. 若调整后不合适，点击“一键居中到默认位置”。
5. 确认后点击“保存本机校准”，否则重启后不保留新位置。
6. 从错误鼓凳锚点版本升级时，应用会自动忽略旧的整体/逐鼓面位置偏移，但保留手势和延迟参数。

## 3. 握槌阈值

1. 选择“校准与设置”→“手势与声音”。
2. 自然握住想象中的鼓槌，保持手腕放松，点击“记录握姿”。
3. 完全张手，点击“记录张手”。
4. 应用取两种姿态的捏合距离和平均弯指距离中点作为阈值。
5. 左右手各重复三次；若左右手差异明显，以更容易误触的一侧为准手动微调。

## 4. 敲击速度

1. 对 Snare 连续做轻、中、重敲击，各至少 6 次。
2. 面板会收集鼓面穿越速度，最多保留最近 20 个样本。
3. 样本不少于 12 个后点击“采用速度样本”。
4. 应用将低分位速度的 72% 作为最低命中速度，将高分位速度作为满力度速度。
5. 用极慢回槌检查误触，再用轻敲检查漏击；必要时手动调整。

## 5. 鼓面位置

1. 进入“鼓面位置”，使用“上一个/下一个”选择鼓件。
2. 校准模式会显示半透明碰撞面；正常演奏时碰撞面隐藏，只在命中时闪现。
3. 分别调整 X（左右）、Y（上下）、Z（前后），让碰撞面贴合模型鼓皮或镲片。
4. 建议顺序：Snare → Hi-Hat → 两个 Rack Tom → Floor Tom → Crash → Ride → Kick。
5. 每件鼓先调中心，再用边缘敲击确认半径；当前面板只调整中心，半径沿用模型配置。

## 6. 演奏延迟

应用显示的“应用内延迟”是从手追踪帧进入应用到 `SoundPool.play` 成功提交的时间，只覆盖软件链路，不包含系统混音、扬声器和空气传播。

真机验收需要同时记录：

- 面板中的应用内延迟：连续 30 次命中的滑动平均。
- 声学端到端延迟：用 120 fps 或更高帧率相机同时拍到鼓槌穿面和耳机/扬声器波形指示，或使用外部触发麦克风测量。
- 目标：应用内延迟稳定、没有周期性尖峰；声学端到端以实际设备、系统版本和输出设备为准记录，不用模拟器替代。

完成上述步骤后点击“保存本机校准”。参数保存在该设备应用私有存储中；卸载应用会清除它们。

## 7. 验收记录模板

```text
设备型号：
PICO OS 版本：
应用版本：1.0
是否使用蓝牙音频：
最低敲击速度（m/s）：
满力度速度（m/s）：
捏合阈值（mm）：
弯指阈值（mm）：
手势敲击延伸长度（mm）：
应用内延迟均值（ms）：
声学端到端延迟（ms）：
误触/漏击备注：
```
